fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
sqlmodel>=0.0.22
# Milvus Lite（嵌入式向量库）：与 pymilvus 配合，向 MilvusClient 传入本地 .db 文件路径即可启用。
# milvus-lite 3.x 为纯 Python 实现，Windows / Linux / macOS 均可安装（旧版 2.x 才仅限 Linux/macOS）。
pymilvus>=3.0.0,<4.0
milvus-lite>=3.0.0,<4.0
openai>=1.40
httpx>=0.27
PyYAML>=6.0
